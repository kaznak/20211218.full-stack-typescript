{
  "name": "root",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "format": "run-s format:package.json format:prettier",
    "format:package.json": "sort-package-json",
    "format:prettier": "prettier -u -w ."
  },
  "lint-staged": {
    "*": [
      "prettier --ignore-unknown --write"
    ]
  },
  "devDependencies": {
    "husky": "^7.0.4",
    "lerna": "^4.0.0",
    "lint-staged": "^12.1.2",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.5.1",
    "sort-package-json": "^1.53.1"
  }
}
